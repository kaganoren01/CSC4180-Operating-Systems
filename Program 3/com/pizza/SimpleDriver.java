package com.pizza;

/**
 * Simple example driver
 */
public class SimpleDriver {
  public static void main(String[] args) throws InterruptedException {
    // Initialize the monitor
    // final Buffet buffet = new BuffetMonitor(20);

    // Make and start a few actor threads
    // Thread eater = new Thread(() -> System.out.println("Eater: " + buffet.TakeAny(1)));
    // Thread baker = new Thread(() -> System.out.println("Baker: " + buffet.AddPizza(1, SliceType.Cheese)));
    // eater.start();
    // baker.start();

    // Waiting until all threads done
    // eater.join();
    // baker.join();
  }
}
